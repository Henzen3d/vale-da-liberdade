# Handoff 2026-09-09 — Integração da Skill `descricoes-vale-liberdade` & Pipeline YouTube

## 1. O que foi feito na sessão
- **Criação e Adaptação da Skill `descricoes-vale-liberdade`:**
  - Definida e sincronizada em `SKILLS/descricoes-vale-liberdade/SKILL.md` e `.hermes/skills/content/descricoes-vale-liberdade/SKILL.md` (no host `/home/osmar/.hermes/skills/content/descricoes-vale-liberdade/SKILL.md`).
  - Indexada no Hermes Agent via comando e gravada na memória de longo prazo (Mnemosyne).
- **Novo Motor Python (`scripts/description_optimizer.py`):**
  - Implementado motor com suporte a Gemini (MultiClient / rotação de chaves), OpenRouter (modelos free) e fallback determinístico de alta qualidade.
  - Compliance estrito com scrubbing determinístico de URLs do canal ANCAPSU (`@ancap_su`, `ancap.su`).
  - Injeção de link canônico do app PWA (`https://news.mob.tec.br`), controle de tamanho (300–700 chars no corpo, máx ~1.000 total) e 0 a 3 hashtags.
  - Suporte a episódio diário (`--date YYYY-MM-DD`) gravando em `episodes/{date}-description.txt` e especial Brasil & Mundo (`--video-id ID`) atualizando `especial-{id}.json`.
- **Integração aos Pipelines:**
  - `scripts/pipeline.py`: Adicionada **Etapa 2.6/8** no `cmd_full()` executando `scripts/description_optimizer.py` de forma não-bloqueante; persistência de `youtube_description` no `episodes/{date}-metadata.json` via `cmd_process()`.
  - `scripts/youtube_video_generator.py`: Atualizado `resolve_assets(date, return_description=True)` para ler `episodes/{date}-description.txt`, sincronizando título e descrição para renderização e upload.
- **Documentação e Prompts Operacionais:**
  - Criado `prompts/hermes_agent_description_prompt.md` com templates canônicos para execução pelo Hermes Agent.
  - Atualizado `AGENT_GUIDE.md` documentando as etapas 2.5 e 2.6 do pipeline.
- **Testes Automatizados:**
  - Criada suíte `scripts/test_description_optimizer.py` (6/6 testes OK).
  - Testes de regressão `scripts/test_turguniev_scrub.py` e `scripts/test_youtube_channel_policy.py` validados (28/28 OK).

## 2. Decisões Tomadas
1. **Regra ANCAPSU:** Nenhuma URL, link ou menção a canais do ANCAPSU pode aparecer nas descrições públicas do Vale da Liberdade. A limpeza é feita deterministicamente via regex antes de qualquer saída.
2. **Personas no Estúdio:** Na bancada do podcast e nos vídeos, o âncora libertário é "Peter Albuquerque" (ao lado de Ricardo Souto). O nome Peter Turguniev é reservado exclusivamente a contexto editorial amplo/criador da rede, nunca como locutor.
3. **Padrão Não-Bloqueante:** O gerador de descrições segue o padrão dos geradores de título e thumbnail — se qualquer API externa falhar, o pipeline aciona o fallback determinístico contextual e segue a produção sem travar.

## 3. Estado Atual dos Artefatos
- **Skill:** Ativa e testada pelo Hermes Agent (`skill_view` resolve em `content/descricoes-vale-liberdade`).
- **Motor:** `scripts/description_optimizer.py` compilado e funcional em modo dry-run e produção.
- **Pipeline:** `scripts/pipeline.py` pronto com etapas 2.5 (Título) e 2.6 (Descrição).
- **Áudio de Teste do Hermes:** `/home/osmar/.hermes/cache/audio/tts_20260909_193155_764895.mp3`.

## 4. Próximos Passos Planejados
1. Executar um ciclo completo do pipeline diário (`pipeline.py full --date YYYY-MM-DD`) em ambiente de homologação/produção para verificar a geração de ponta a ponta dos arquivos `{date}-title.txt`, `{date}-description.txt` e `{date}-metadata.json`.
2. Monitorar a integração com o pipeline horário de Brasil & Mundo (`bm-hourly-pipeline.sh`) utilizando `--video-id`.
3. Validar se os uploads automáticos do YouTube incluem título e descrição gerados sem discrepâncias.
