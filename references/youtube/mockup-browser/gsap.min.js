/* stub local — evita 404 do http.server no record_mockup; API mínima usada pelo mockup */
(function (g) {
  function chain() {
    return {
      fromTo: function () { return this; },
      to: function () { return this; },
      set: function () { return this; }
    };
  }
  g.gsap = g.gsap || {
    fromTo: function () {},
    to: function () {},
    set: function () {},
    timeline: function () { return chain(); }
  };
})(typeof window !== "undefined" ? window : globalThis);
