(()=>{let audio=null;let currentEpisode=null;let _pendingEpisode=null;function initPlayer(){audio=document.getElementById("audioEl");if(!audio){audio=document.createElement("audio");audio.id="audioEl";audio.preload="none";document.body.appendChild(audio);}
const events=["play","pause","timeupdate","ended","loadedmetadata","durationchange","error"];events.forEach(evt=>{audio.addEventListener(evt,(e)=>{window.dispatchEvent(new CustomEvent("playerevent",{detail:{type:evt,currentTime:audio.currentTime,duration:audio.duration||0,paused:audio.paused,episode:currentEpisode,error:audio.error}}));});});audio.addEventListener("ended",()=>{if(typeof window.WakeLock==='object'&&window.WakeLock.release){window.WakeLock.release();}});audio.addEventListener("play",()=>{if(typeof window.WakeLock==='object'&&window.WakeLock.request){window.WakeLock.request();}});audio.addEventListener("pause",()=>{if(typeof window.WakeLock==='object'&&window.WakeLock.release){window.WakeLock.release();}});document.addEventListener("visibilitychange",()=>{if(document.visibilityState==="visible"&&!audio.paused){if(typeof window.WakeLock==='object'&&window.WakeLock.request){window.WakeLock.request();}}});}
function resolveAudioUrl(url){if(!url)return"";if(url.startsWith("http://")||url.startsWith("https://")||url.startsWith("blob:"))return url;if(url.startsWith("./"))url=url.slice(1);if(!url.startsWith("/"))url="/"+url;try{return new URL(url,window.location.origin).href;}catch{return url;}}
function load(episode){if(!episode||!episode.audio_url)return;if(document.prerendering){_pendingEpisode=episode;return;}
const sameEpisode=currentEpisode&&currentEpisode.id===episode.id;currentEpisode=episode;const src=resolveAudioUrl(episode.audio_url);if(!sameEpisode||audio.src!==src){audio.src=src;audio.load();}}
function play(episode){if(episode){load(episode);}
if(!audio||!audio.src)return;const p=audio.play();if(p&&typeof p.then==="function"){p.catch(err=>{console.warn("audio.play notice:",err.message||err);});}
if(navigator.mediaSession&&currentEpisode){try{const epArt=currentEpisode?.cover_url_abs||currentEpisode?.cover_url||(currentEpisode?.date?`${location.origin}/thumbnails/${currentEpisode.date}/ep_${currentEpisode.date}.webp`:null);navigator.mediaSession.metadata=new MediaMetadata({title:currentEpisode.title||`Edição ${currentEpisode.date}`,artist:currentEpisode.type==="especial"?"Peter (Solo)":"Peter & Ricardo",album:currentEpisode.type==="especial"?"Brasil & Mundo":"Web Jornal",artwork:[...(epArt?[{src:epArt,sizes:"512x512",type:"image/webp"}]:[]),{src:"/icons/icon-192.png",sizes:"192x192",type:"image/png"},{src:"/icons/icon-512.png",sizes:"512x512",type:"image/png"}]});navigator.mediaSession.setActionHandler("play",()=>play());navigator.mediaSession.setActionHandler("pause",()=>pause());navigator.mediaSession.setActionHandler("seekbackward",(details)=>skip(details.seekOffset||-15));navigator.mediaSession.setActionHandler("seekforward",(details)=>skip(details.seekOffset||15));}catch(err){console.warn("MediaSession initialization failed:",err);}}}
function pause(){if(audio)audio.pause();}
function togglePlay(episode){if(!audio||!audio.src){if(episode){play(episode);}
return;}
if(episode&&currentEpisode&&currentEpisode.id!==episode.id){play(episode);return;}
if(audio.paused){play();}else{pause();}}
function seek(percentage){if(!audio)return;const dur=audio.duration;if(Number.isFinite(dur)&&dur>0){const pct=Math.max(0,Math.min(100,Number(percentage)||0));audio.currentTime=(pct/100)*dur;}}
function skip(seconds){if(!audio)return;const dur=audio.duration;const cur=audio.currentTime||0;const delta=Number(seconds)||0;if(Number.isFinite(dur)&&dur>0){audio.currentTime=Math.max(0,Math.min(dur,cur+delta));}else{audio.currentTime=Math.max(0,cur+delta);}}
function setPlaybackRate(rate){if(audio){audio.playbackRate=parseFloat(rate);}}
function getPlaybackRate(){return audio?audio.playbackRate:1.0;}
function getCurrentEpisode(){return currentEpisode;}
function getAudioState(){return{currentTime:audio?audio.currentTime:0,duration:audio?audio.duration:0,paused:audio?audio.paused:true,playbackRate:audio?audio.playbackRate:1.0,episode:currentEpisode};}
if(document.readyState==="loading"){document.addEventListener("DOMContentLoaded",initPlayer);}else{initPlayer();}
if(document.prerendering){document.addEventListener("prerenderingchange",()=>{const ep=_pendingEpisode;_pendingEpisode=null;if(ep)load(ep);});}
window.PlayerManager={load,play,pause,togglePlay,seek,skip,setPlaybackRate,getPlaybackRate,getCurrentEpisode,getAudioState};})();